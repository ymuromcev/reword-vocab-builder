---
id: BL-10
title: "Two-example sentences + RU translation generation per word"
status: done
priority: P1
tier: M
created: 2026-05-19
closed: 2026-05-19
tags: [examples, translation, llm]
---

## Context

Для каждого слова нужно: ru_translation + 2 английских примера + их
русские переводы. Это 5 из 7 колонок CSV. Качество промпта = качество
тула.

## Образ результата

```python
out = enrich({"word": "to leverage"})
# {
#   "word": "to leverage",
#   "ru": "использовать (по максимуму)",
#   "ex1_en": "We need to leverage existing customer data here.",
#   "ex1_ru": "Нужно по максимуму использовать существующие данные о клиентах.",
#   "ex2_en": "Let's leverage our existing partnerships first.",
#   "ex2_ru": "Давай сначала используем наши существующие партнёрства.",
# }
```

## Plan

- [x] `src/enricher.py` (новый): `enrich(word_obj, *, llm) -> dict`
      + `enrich_many(words, *, llm) -> list[dict]`.
- [x] Промпт-конструктор:
      - role: native English + native Russian translator.
      - constraints:
        * 2 примера разной длины (короткий + средний).
        * примеры показывают разный context use слова.
        * RU перевод естественный, не дословный.
        * без символов ($, %, →, +, /, K, x, currency) в английских
          примерах (для проговаривания вслух).
- [x] Source-mode bonus: если есть `source_quote` в word_obj,
      второй пример просим адаптировать из реальной цитаты.
- [x] Тесты: 9 кейсов в `tests/test_enricher.py` с fake LLM
      (все 5 полей, source_quote в user message, retry-on-symbol,
      два страйка → EnrichmentError, batching 25 → 2 calls,
      сохранение лишних input-ключей).

## Progress

- 2026-05-19: implemented per RFC-010. `enrich` и `enrich_many`
  с inject'ом LLM-клиента (`LLMClient` Protocol), forbidden-symbol
  retry (max 2 вызова), батчи по 20 слов в `enrich_many`. Тесты
  проходят локально.

## Definition of Done

- Все 5 полей заполнены валидно.
- Запрещённые символы (`$`, `%`, `→`, `+`, `/`, `K`, `x`)
  отсутствуют в английских примерах.
- Тесты проходят.

## NOT in scope

- Локализация на другие языки кроме русского (P3).
- ML-генерация (только LLM сейчас).
