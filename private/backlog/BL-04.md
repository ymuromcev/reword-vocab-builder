---
id: BL-04
title: "Verb detector + 'to ' prefix rule"
status: done
priority: P0
tier: M
created: 2026-05-19
closed: 2026-05-19
tags: [verbs, formatting, core]
---

## Context

В Reword глаголы хранятся с префиксом `to ` (`to leverage`, `to align`,
`to circle back`). Иначе они некорректно произносятся и не матчатся с
существующим словарём юзера. Нужно автоматически определять глаголы и
ставить префикс.

## Образ результата

Чистая функция:

```python
is_verb("leverage")          # True
is_verb("trade-off")         # False
is_verb("circle back")       # True (phrasal verb)
is_verb("running")           # False (gerund, не base form)

to_prefix("leverage")        # "to leverage"
to_prefix("trade-off")       # "trade-off"
to_prefix("to align")        # "to align" (уже с префиксом)
```

## Plan

- [x] `src/verb_detector.py`: определить базовую форму глагола.
- [x] Подход: spaCy POS-tagging (`VB` = base form). WordNet/NLTK
      выкинуты — см. RFC-004, single-dep policy.
- [x] Edge cases:
      - phrasal verbs (`push back`, `dive in`) — particle allow-list.
      - идиомы (`raise the bar`, `bake in`) — first-token VB rule.
      - gerunds (`running`, `building`) — НЕ префиксить.
      - participles (`built`, `running`) — НЕ префиксить.
- [x] `to_prefix(word)` — идемпотентный.
- [x] Tests на синтетическом наборе через fake POS-tagger (полностью
      hermetic, без `en_core_web_sm` в CI). Cherry-picked сет на ~80
      слов с правильным распределением verb/non-verb.

## Done

- `src/verb_detector.py` — pipeline override → strip-`to ` → spaCy POS →
  phrasal → idiom.
- `config/verb_overrides.yaml` — пустой шаблон + комментарии.
- `tests/test_verb_detector.py` — 80+ тест-кейсов, fake POS-tagger
  через `set_nlp`. Покрытие: 36 verbs, 35 non-verbs, 10 phrasals,
  5 idioms, idempotency, override precedence.
- `pyproject.toml` — `spacy`, `pyyaml`.
- `rfc/004-verb-detector.md` — approved RFC.

## Definition of Done

- На сете из 200+ слов precision/recall глагольной детекции > 95%.
- Phrasal verbs детектятся.
- Идемпотентность гарантирована.
- Тесты покрывают edge cases.

## NOT in scope

- Префикс для существительных (`a/an/the`) — Reword не требует.
- Грамматические правила для других частей речи.

## Notes

Подсказка по архитектуре: можно сделать конфигурируемый "manual override"
файл (`config/verb_overrides.yaml`) — список слов, которые модель
неправильно классифицирует. Полезно если spaCy ошибается на новых
идиомах.
