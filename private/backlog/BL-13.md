---
id: BL-13
title: "Test suite for deterministic pieces"
status: done
priority: P1
tier: M
created: 2026-05-19
closed: 2026-05-20
refs: [BL-02, BL-04, BL-05, BL-06]
tags: [tests, quality]
---

## Context

Тулза легко скатывается в "генерирует фигню" — без покрытия
детерминированных частей будет тяжело отлаживать. Покрываем то, что
не зависит от LLM.

## Образ результата

`pytest` проходит зелёным. Покрытие:

- `backup_reader.read_backup()` + `classify()` — фикстурный SQLite.
- `verb_detector.is_verb()` + `to_prefix()` — 50+ слов из эталона.
- `csv_writer.write_csv()` — round-trip и побайтовое сравнение.
- `dedup.dedup()` — синтетические сценарии (mastered, active-long, etc).

## Plan

- [x] `tests/fixtures/reword_test.backup` — мини-SQLite с
      контролируемыми значениями. (shipped by layer-1)
- [x] `tests/fixtures/known_good.csv` — эталонный CSV для сравнения.
      (shipped by layer-1)
- [x] `tests/test_backup_reader.py`. (shipped by layer-1)
- [x] `tests/test_verb_detector.py`. (shipped by layer-1)
- [x] `tests/test_csv_writer.py`. (shipped by layer-1)
- [x] `tests/test_dedup.py` (заменить плейсхолдер). (shipped by layer-1)
- [x] `tests/test_integration_smoke.py` — end-to-end smoke по RFC-013.
- [ ] CI workflow (GitHub Actions) — optional, P2.

## Progress

- 2026-05-20 — добавлен `tests/test_integration_smoke.py` с тремя
  тест-функциями (`test_pipeline_produces_expected_csv`,
  `test_pipeline_drops_mastered`,
  `test_pipeline_applies_to_prefix_only_to_verbs`). Покрывает связку
  `verb_detector.to_prefix` → `dedup.dedup` / `dedup_only` (по
  контракту RFC-006) → `csv_writer.write_csv`. spaCy подменяется
  in-process через `verb_detector.set_nlp`; `backup_index`
  собирается напрямую как `dict[str, ClassifiedWord]` — без
  SQLite-файла (BL-02 покрыт отдельно). Enricher замокан хардкод-
  данными, чтобы прогнать write-step без LLM/CMU. Per-модульные
  юнит-тесты отмечены как пришедшие из layer-1.
- 2026-05-20 — CI workflow оставлен `[ ]` (out of scope по RFC, P2).

## Definition of Done

- `pytest` зелёный.
- Покрытие core-модулей > 80%.
- LLM-моки в integration-тестах не делают реальных запросов.

## NOT in scope

- E2E тесты с реальным LLM (это smoke-test, делать вручную перед
  релизом).
- CI workflow (GitHub Actions) — P2, отдельным таском.
