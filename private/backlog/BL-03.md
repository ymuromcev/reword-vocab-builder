---
id: BL-03
title: "Google Drive MCP fetcher for reword_en.backup"
status: done
priority: P0
tier: M
created: 2026-05-19
closed: 2026-05-19
refs: [BL-02]
tags: [drive, mcp, integration]
---

## Context

Бэкап Reword лежит на Google Drive юзера (или в iCloud). Тул должен
сам найти последний и скачать его локально, чтобы дальше его прочитал
BL-02. Доступ — через Google Drive MCP-коннектор, без явных токенов.

## Образ результата

Функция:

```python
local_path = fetch_latest_backup()
# → '/tmp/reword_en.backup' (свежий)
```

CLI при старте вызывает её, кеширует на N часов (чтобы не качать
каждый раз), и передаёт путь в BL-02 reader.

## Plan

- [x] `src/drive_mcp.py`: поиск файла `reword_en.backup` на Drive
      (по имени или конфигурируемой папке).
- [x] Скачивание во временный файл / кеш-директорию
      `~/.cache/reword-vocab-builder/`.
- [x] TTL кеша (по умолчанию 6 часов — конфигурируется).
- [x] Конфиг через ENV: `REWORD_BACKUP_PATH` для override (если юзер
      хочет указать локальный путь напрямую, минуя Drive).
- [x] Тест с mock'ом Drive MCP.

## Definition of Done

- Скрипт скачивает свежий бэкап если кеш протух.
- Скрипт возвращает локальный путь.
- Override через ENV работает.
- Тест проходит на mock-ответе MCP.

## NOT in scope

- Загрузка обратно на Drive.
- Любые операции записи / удаления на Drive.

## Open questions

- Где именно у Jared лежит `reword_en.backup` на Google Drive —
  спросить путь или поиск по имени.
- iCloud-вариант (`~/Library/Mobile Documents/iCloud~ru~poas~englishwords/Documents/reword_en.backup`)
  — поддерживаем как fallback или нет?

## Done

- 2026-05-19: `fetch_latest_backup()` shipped per RFC-003. Resolution
  order env → iCloud (mac only) → Drive MCP. Atomic write to
  `~/.cache/reword-vocab-builder/reword_en.backup`. TTL via mtime,
  default 6h, `force=True` skips check. `DriveUnavailableError` when
  no MCP client and no fallback. Drive interaction behind injected
  `DriveMCPClient` Protocol — tests use a fake, no real network.
- Tests: `tests/test_drive_mcp.py` (env override, iCloud darwin /
  non-darwin, Drive search picks newest, atomic .tmp cleanup, missing
  client, empty search, fresh / stale / forced TTL).
