---
id: BL-14
title: "README usage examples with sample outputs"
status: done
closed: 2026-05-20
priority: P2
tier: XS
created: 2026-05-19
refs: [BL-11]
tags: [docs, readme]
---

## Context

После того как CLI готов, README нужно дополнить реальными примерами
запусков с реальным output'ом — чтобы человек смог сразу понять, что
получит на выходе.

## Образ результата

В README:

- Топовый раздел "Quick start" — 3 минуты, человек понимает, что и
  как.
- Реальный output из stdout (progress lines).
- Пример CSV-строки в формате Reword.
- Раздел "Troubleshooting" — каждое возможное сообщение об ошибке
  с инструкцией как починить.

## Plan

- [x] Quick start: install → API key → topic-команда → source-команда.
- [x] Stdout пример (что увидишь в терминале по ходу прогона).
- [x] Пример строки CSV (7 колонок, semicolon, QUOTE_ALL).
- [x] Полная таблица options (`--target-count`, `--output`,
      `--backup-path`).
- [x] Section "Backup file resolution" — порядок резолва (`--backup-path` →
      `REWORD_BACKUP_PATH` → iCloud → Drive MCP).
- [x] Section "Using via Claude Code" — закрывает BL-12 на стороне README.
- [x] Section "Troubleshooting" — все 6 типов user-facing ошибок CLI
      с инструкцией.
- [x] Install: `pip install -e .` + `python -m spacy download en_core_web_sm`.

## Definition of Done

- [x] README имеет Quick start и Troubleshooting.
- [x] В Quick start есть пример output'а.
- [x] README не хвалит то, что не реализовано (помечено что PyPI
      release будет в BL-15).

## Progress

- 2026-05-20: README переписан под живой CLI. Все примеры команд
  и stdout — реальные (соответствуют тому, что печатает `src/cli.py`).
  Раздел Troubleshooting покрывает все 6 exit-2 сообщений из RFC-011.

## NOT in scope

- Видео-демо.
- Скриншоты Reword (требуют live setup).
- Полная документация API.
